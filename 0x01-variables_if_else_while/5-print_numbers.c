#include <stdio.h>

/**
 * main - entry of a program tht print a single digit number of base 10
 * Return: always 0 (succssor)
 */

int main(void)
{
	int tee;

	for (tee = 0; tee < 10; tee++)
	printf("%d", tee);
	printf("\n");
	return (0);
}
