#include <stdio.h>

/**
 * main - entery to a program that prints all single digit number of base 10
 * Return: alwats 0 (success)
 */

int main(void)
{
	int tee;
	for (tee = 48; tee < 58; tee++)
{
	putchar(tee);
}
	putchar('\n');
	return  (0);
}
