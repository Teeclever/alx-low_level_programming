#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - my entry point to print alphabat in lowercase
 * Return: ALways 0 (success)
*/

int main(void)
{
    char te;
    for(te ='a'; te <= 'z' te++)
}
{
	putchar(\n);
}
	putchar('\n');
	return (0)
