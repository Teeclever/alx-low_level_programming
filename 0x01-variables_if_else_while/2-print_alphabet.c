#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - my entry point to print alphabat in lowercase
 * Return: ALways 0 (success)
*/

int main(void)
{
	char ta;

	for (ta = 'a'; ta <= 'z'; ta++)

	putchar(ta);

	putchar('\n');
	return (0);
}
